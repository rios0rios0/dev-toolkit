kind: 'Fixed'
body: 'removed the stale "update the version in `src/main.go`" item from the bump pull request checklist: there is no `src/` directory, and the build version is injected by the `Makefile` through `-X main.version=$(VERSION)` from the latest Git tag, so there is nothing to edit by hand'
time: '2026-08-25T22:37:23.607706439Z'
